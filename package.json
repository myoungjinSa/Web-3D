{
  "name": "web-3d",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:wasm": "emcc cpp/color_picker.cpp -O3 -s MODULARIZE=1 -s EXPORT_NAME=ColorPickerModule -s EXPORTED_FUNCTIONS=\"['_next_color_index']\" -s EXPORTED_RUNTIME_METHODS=\"['cwrap']\" -o public/wasm/color_picker.js"
  },
  "dependencies": {
    "three": "^0.164.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
